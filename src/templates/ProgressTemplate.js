// import React from 'react';
// import './style.css';
// import { progressicon } from '../components/Chore';

// const ProgressTemplate = ({ bars }) => {
//     return (
//         <div>
//             {bars.map((bar, index) => (
//                 <div key={index} className={`progress__step progress__step--${bar}`}>
//                     {progressicon(bar, index)}
//                 </div>
//             ))}
//         </div>
//     );
// };

// export default ProgressTemplate;
